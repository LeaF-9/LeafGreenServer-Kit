options:
	prefix: §9[§bFFA§9]§r

function ffaMsg(msg: text):
	loop all players:
		set {_wc} to "%world of location of loop-player%"
		if {_wc} contains "FFA":
			message formatted "{@prefix} {_msg}" to loop-player
