command /blockedit <player=%player%>:
	aliases: /be
	permission: sk.admin
	trigger:
		if isBlockEditable(arg 1) is true:
			setBlockEditable(arg 1, false)
			message "§cブロック編集を不可能にしました" to arg 1
		else:
			setBlockEditable(arg 1, true)
			message "§aブロック編集を可能にしました" to arg 1