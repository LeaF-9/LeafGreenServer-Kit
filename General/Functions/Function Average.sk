function average(nums: numbers) :: number:
	set {_sum} to 0
	set {_length} to size of {_nums::*}
	loop {_nums::*}:
		set {_num} to loop-value
		add {_num} to {_sum}
	set {_average} to {_sum} / {_length}
	return {_average}