function checkEntityType(entity: entity, type: text) :: boolean:
	set {_return} to false
	broadcast "%{_entity}.getType()%"
	broadcast "%{_type}%"
	if {_entity}.getType() is {_type}:
		set {_return} to true
	return {_return}
